# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase cli cache
/supabase/.temp/
/supabase/.branches/

# Claude Code session exports (never commit)
*-session.md
claude-export-*.txt
*-task-*.txt

# Scratch verification scripts. Task 8 leaked `_verify_tmp.ts` into a
# commit via `git add -A`; the underscore prefix is the convention for
# throwaways that live in the repo root only so they can resolve
# node_modules (tsx cannot from /tmp).
_*.ts
_*.mjs
